<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml"
              xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
              xmlns:Engine="clr-namespace:VirtualVector.Scene_View;assembly=VirtualVector" 
               xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
              xmlns:colors="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0">
    <Grid Background="#1E1E1E">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <StackPanel Grid.Column="0" Margin="12">
            <TextBlock Text="ENGINE INTERFACE" Foreground="White" FontWeight="Bold" Margin="0,0,0,12"/>
            <Button Content="▶ PLAY GAME" Command="{Binding EngineGuiCommand}" Height="32" Margin="0,0,0,6" Background="#2E7D32" Foreground="White"/>
            <Button Content="⏹ STOP EDITOR" Command="{Binding StopEditorCommand}" Height="32" Margin="0,0,0,16" Background="#C62828" Foreground="White"/>
            <TextBlock Text="HIERARCHY" Foreground="#AAAAAA" FontSize="11" Margin="0,8,0,4"/>
        </StackPanel>

        <!-- Viewport placeholder — WebView2 will be parented here -->
        <Border Grid.Column="1" Background="#111111" BorderBrush="#333333" BorderThickness="1" Margin="4">
            <DockPanel>
                <Engine:EngineGui/>
            </DockPanel>
            
        </Border>
    </Grid>
</DataTemplate>
